import { Module } from 'lib/types'

/**
 *
 * This module is intended for handling some specific phrases to give a predefined response to them
 */
export const ReplyModule: Module = ({ bot, chatId }) => {
}
